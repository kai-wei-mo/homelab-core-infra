replicaCount: 1

image:
  repository: docker.io/jellyfin/jellyfin
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8096

persistence:
  config:
    enabled: true
    size: 5Gi

  media:
    enabled: true
    type: hostPath
    hostPath: /mnt/seagate/jellyfin

  cache:
    enabled: false

ingress:
  enabled: true
  className: traefik
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
  hosts:
    - host: jellyfin.home.chaewon.io
      paths:
        - path: /
          pathType: Prefix
    - host: jellyfin.chaewon.io
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: home-chaewon-io-wildcard
      hosts:
        - jellyfin.home.chaewon.io

networkPolicy:
  enabled: false
