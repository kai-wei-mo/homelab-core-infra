apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cert-manager

resources:
  - install.yaml

  - sealed-cloudflare-api-token.yaml
  - cluster-issuer.yaml
  - certificate.yaml
