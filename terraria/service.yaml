apiVersion: v1
kind: Service
metadata:
  name: terraria
  labels:
    app: terraria
spec:
  type: LoadBalancer
  selector:
    app: terraria
  ports:
    - name: terraria
      port: 7777
      targetPort: terraria
      protocol: TCP
